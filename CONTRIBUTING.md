# 貢獻指南

感謝你考慮為社團管理系統做出貢獻！

## 如何貢獻

### 報告 Bug

如果你發現 bug，請在 GitHub Issues 中建立新的 issue，並包含：

- 清楚的標題和描述
- 重現步驟
- 預期行為 vs 實際行為
- 環境資訊（OS、Node.js 版本、資料庫版本）
- 錯誤訊息或截圖

### 提出新功能

如果你有新功能的想法：

1. 先檢查 Issues 是否已有人提出
2. 建立新的 Feature Request issue
3. 詳細描述功能和使用情境
4. 討論實作方式

### Pull Request 流程

1. **Fork 專案**
   ```bash
   # 在 GitHub 上點擊 Fork 按鈕
   ```

2. **Clone 你的 fork**
   ```bash
   git clone https://github.com/你的使用者名稱/club-management-system.git
   cd club-management-system
   ```

3. **建立功能分支**
   ```bash
   git checkout -b feature/amazing-feature
   # 或
   git checkout -b fix/bug-fix
   ```

4. **進行變更**
   - 遵循現有的程式碼風格
   - 加入適當的註解
   - 更新相關文件

5. **測試你的變更**
   ```bash
   cd backend
   npm test  # 如果有測試的話
   ```

6. **Commit 變更**
   ```bash
   git add .
   git commit -m "feat: 加入新功能描述"
   # 或
   git commit -m "fix: 修復某個 bug"
   ```

   Commit message 格式：
   - `feat:` 新功能
   - `fix:` bug 修復
   - `docs:` 文件變更
   - `style:` 程式碼格式調整
   - `refactor:` 重構
   - `test:` 測試相關
   - `chore:` 其他雜項

7. **Push 到你的 Fork**
   ```bash
   git push origin feature/amazing-feature
   ```

8. **建立 Pull Request**
   - 到 GitHub 上你的 fork
   - 點擊 "New Pull Request"
   - 填寫 PR 描述
   - 等待審核

## 程式碼風格

### JavaScript/Node.js

- 使用 ES6+ 語法
- 使用 2 空格縮排
- 使用有意義的變數名稱
- 加入必要的註解
- 避免過長的函數（< 50 行）

### 命名規範

- 檔案名稱：`kebab-case.js`
- 變數/函數：`camelCase`
- 常數：`UPPER_SNAKE_CASE`
- 類別：`PascalCase`

### API 設計

- RESTful 設計原則
- 使用適當的 HTTP 方法
- 清楚的錯誤訊息
- 一致的回應格式

## 資料庫變更

如果需要修改資料庫結構：

1. 更新 `database/schema.sql`
2. 建立 migration script（未來）
3. 更新相關的 API 文件
4. 測試向後相容性

## 文件

請確保更新相關文件：

- `README.md` - 如果加入新功能
- `backend/README.md` - 如果變更 API
- API 端點註解
- 程式碼內註解

## 審核流程

PR 提交後：

1. 自動檢查會運行（如果有設定 CI/CD）
2. 維護者會審核程式碼
3. 可能會要求修改
4. 通過後會被合併

## 行為準則

- 尊重所有貢獻者
- 建設性的討論
- 專注於改善專案
- 歡迎新手

## 需要幫助？

- 查看 [Issues](https://github.com/你的使用者名稱/club-management-system/issues)
- 閱讀 [文件](./README.md)
- 在 Discussions 中提問

感謝你的貢獻！ 🎉
